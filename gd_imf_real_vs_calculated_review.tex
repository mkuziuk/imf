\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{array}
\usepackage{graphicx}
\usepackage{float}
\usepackage[colorlinks=true,linkcolor=blue,urlcolor=blue,citecolor=blue]{hyperref}

\title{Real-vs-Calculated IMF Comparison}
\author{}
\date{}

\begin{document}
\maketitle

\section{Task}

The notebook
\href{https://github.com/mkuziuk/imf/blob/main/gd_imf_real_vs_calculated.ipynb}{gd\_imf\_real\_vs\_calculated.ipynb}
extends the gradient-descent IMF experiment from
\texttt{gd\_irmf.ipynb}. The signal, observation generator, window schedule,
kernel, robust contrast, lookup grid, and robust gradient-descent implementation
follow the same experiment structure. The clean signal is now a reproducible,
less-structured synthetic signal with baseline, chirp, transient, and texture
components. The goal is to compare a clean-reference IMF with the IMF calculated
from a noisy observation.

The observation model is
\[
Y_t = X_t + \varepsilon_t,
\]
where \(X_t\) is the clean signal. The clean-reference, or ``real'', IMF is always
computed from \(X_t\). The calculated IMF is computed from an observation generated
from the same signal and noise settings. The notebook compares two cases:
\[
\begin{array}{ll}
\text{No contamination:} & \text{clean linear IMF vs. observed linear IMF}, \\
\text{With contamination }p=0.2: & \text{clean robust IMF vs. observed robust IMF}.
\end{array}
\]
Thus the clean reference uses the same contrast as the estimator in each case.\newline

There are two different error quantities in the notebook.

\begin{enumerate}
\item The \emph{recursive IMF-component error} is the error in the component that
is actually extracted at stage \(k\):
\[
e_k(t)=\widetilde S^{(k)}_{\mathrm{calculated}}(t)
-\widetilde S^{(k)}_{\mathrm{real}}(t).
\]
This is the error shown in the IMF grids. It is not just the stage-\(k\) smoother
applied to the original noise, because stage \(k\) is fitted to the current
residual after stages \(1,\ldots,k-1\) have already been subtracted. In the linear
case, if \(W_{h_k}\) is the weighted-mean smoother at bandwidth \(h_k\), then
\[
e_1 = W_{h_1}\varepsilon,
\qquad
e_k =
W_{h_k}(I-W_{h_{k-1}})\cdots(I-W_{h_1})\varepsilon
\quad (k>1).
\]

\item The \emph{single-pass error} is the diagnostic error obtained by applying
the stage-\(k\) smoother directly to the original clean and noisy observations,
without recursive residual subtraction. In the linear case this quantity is
\[
d_k = W_{h_k}\varepsilon.
\]
This is the quantity showing that the error grows as the window size decreases.
\end{enumerate}

The notebook reports both \(e_k\) and \(d_k\). The robust case is nonlinear, so
\(d_k\) is computed as the difference between one robust local fit applied to
\(Y\) and one robust local fit applied to \(X\) at the same bandwidth. The same
distinction still applies because \(\widetilde S^{(k)}\) is always fitted to the
current residual \(Y^{(k)}\), not directly to the original observation \(Y\).

For the linear case, the contrast is quadratic,
\[
\rho(r)=\frac{r^2}{2},
\]
so each local fit is the Epanechnikov-weighted mean in the current residual window.
For the robust case, the notebook uses the smooth robust contrast from
\texttt{gd\_irmf.ipynb},
\[
\rho_H(r)
= r\,\operatorname{erf}\left(\frac{r}{\sqrt{2}H}\right)
+ \sqrt{\frac{2}{\pi}}H
\exp\left(-\frac{r^2}{2H^2}\right),
\]
with score
\[
\psi_H(r)=\operatorname{erf}\left(\frac{r}{\sqrt{2}H}\right).
\]

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated/observations.png}
\caption{The two observations used in the comparison. The first has Gaussian noise
only. The second uses contamination probability \(p=0.2\) and contamination scale
\(0.6\), with contaminated points shown in red.}
\end{figure}

\section{Setup and Metrics}

The main parameters match the current run of \texttt{gd\_irmf.ipynb}.

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.20\linewidth}
>{\raggedright\arraybackslash}p{0.30\linewidth}
>{\raggedright\arraybackslash}p{0.42\linewidth}@{}}
\hline
Symbol / name & Notebook value & Meaning \\
\hline
\(n\) & \(\texttt{1000}\) & Number of sample points. \\
\(t\) & \([0,1]\) & Normalized design interval. \\
\(\sigma\) & \(\texttt{0.4}\) & Gaussian noise scale. \\
\(H\) & \(2\sigma = 0.8\) & Robust contrast smoothing scale. \\
\(K\) & Epanechnikov & Kernel used for all local windows. \\
\(a\) & \(\sqrt{2}\) & Window shrink factor used by the schedule. \\
\(a^{-k/2}\) & stage-dependent & Factor used for all scaled error diagnostics,
including RMSE trends, sup-error trends, signed-error boxplots, and IMF-grid
scaled-error panels. \\
window sizes & \begin{tabular}[t]{@{}l@{}}\(501,355,251,177,125,\)\\
\(89,63,45,31\)\end{tabular} & Number of sampled
points in each symmetric local window. \\
radius & \((\texttt{window\_size}-1)/2\) & Discrete half-width. The label
\texttt{window} in the plots refers to \texttt{window\_size}, not radius. \\
contamination & probability \(0.2\), scale \(0.6\), seed \(777\) & Synthetic
contamination settings for the robust comparison. \\
\hline
\end{tabular}
\caption{Main parameters used in the real-vs-calculated IMF notebook.}
\end{table}

For every IMF stage \(k\), the notebook computes
\[
e_k(t)=\widetilde S^{(k)}_{\mathrm{calculated}}(t)
- \widetilde S^{(k)}_{\mathrm{real}}(t),
\]
the recursive scaled error
\[
a^{-k/2}e_k(t)=\frac{e_k(t)}{a^{k/2}}.
\]
It also computes the single-pass diagnostic \(d_k\) and its scaled version
\(a^{-k/2}d_k\). The grid plots show the real IMF, calculated IMF, recursive
error \(e_k\), and recursive scaled error \(a^{-k/2}e_k\). Within each grid, both
IMF columns use one shared min/max \(y\)-axis range, the error column uses its
own min/max range, and the scaled-error column uses its own min/max range.

The scalar error trend for each IMF count \(k\) is the RMSE over the time grid:
\[
\operatorname{RMSE}(e_k)
= \left(\frac{1}{n}\sum_{t=1}^n e_k(t)^2\right)^{1/2}.
\]
For the coefficient summaries, the notebook fits a line
\[
r_k = \beta_1 k + \beta_0
\]
to \(\operatorname{RMSE}(e_k)\), \(\operatorname{RMSE}(d_k)\),
\(\operatorname{RMSE}(a^{-k/2}e_k)\), and
\(\operatorname{RMSE}(a^{-k/2}d_k)\). The RMSE trend plots show these
stage-wise values.

The notebook also reports sup-error diagnostics,
\[
\lVert e_k\rVert_\infty=\max_{1\leq t\leq n}|e_k(t)|,
\qquad
\lVert d_k\rVert_\infty=\max_{1\leq t\leq n}|d_k(t)|.
\]
The scaled sup-error trend also uses \(a^{-k/2}\), so the plotted scaled quantities are
\[
\lVert a^{-k/2}e_k\rVert_\infty
\quad\text{and}\quad
\lVert a^{-k/2}d_k\rVert_\infty.
\]
The error boxplots show signed samples over the time grid, not absolute errors;
the scaled boxplot panels divide the signed values by \(a^{k/2}\).

All four decompositions reconstruct their respective inputs up to numerical
precision:
\[
\left\|Y-\left(\sum_k \widetilde S^{(k)} + Y^{(\mathrm{final})}\right)\right\|_\infty
\approx 8.88\times 10^{-16}.
\]
The realized contamination fraction in the \(p=0.2\) observation is \(0.175\), and
the maximum absolute contamination value is about \(4.043118\).

\section{Notebook Review}

\subsection{No Contamination}

For the no-contamination case, the real IMF is the linear IMF of the clean signal.
The calculated IMF is the linear IMF of the Gaussian-noise-only observation. The
first two grid columns compare these IMFs directly. The third and fourth columns show
\(e_k(t)\) and \(a^{-k/2}e_k(t)\).

\begin{figure}[H]
\centering
\makebox[\linewidth][c]{%
\includegraphics[width=1.12\linewidth,height=0.80\textheight,keepaspectratio]{figures/gd_imf_real_vs_calculated/linear_imf_grid_top.png}%
}
\caption{No-contamination linear comparison for IMF stages 1--5. The left two
columns share one min/max \(y\)-axis range across all stages in the grid.}
\end{figure}

\begin{figure}[H]
\centering
\makebox[\linewidth][c]{%
\includegraphics[width=1.12\linewidth,height=0.72\textheight,keepaspectratio]{figures/gd_imf_real_vs_calculated/linear_imf_grid_bottom.png}%
}
\caption{No-contamination linear comparison for IMF stages 6--9, using the same
column-wise \(y\)-axis ranges as stages 1--5.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated/linear_error_trends.png}
\caption{No-contamination recursive and single-pass error RMSE trends over IMF
count, including all IMF stages.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated/linear_sup_error_trends.png}
\caption{No-contamination recursive and single-pass sup-error trends over IMF
count. The scaled panels use \(a^{-k/2}\).}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated/linear_error_boxplots.png}
\caption{No-contamination signed-error distributions over the time grid. The
scaled panels divide the signed errors by \(a^{k/2}\).}
\end{figure}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.56\linewidth}rr@{}}
\hline
Metric & Slope \(\beta_1\) & Intercept \(\beta_0\) \\
\hline
Recursive IMF \(\operatorname{RMSE}(e_k)\) & \(2.048\times10^{-3}\) & \(8.864\times10^{-3}\) \\
Single-pass \(\operatorname{RMSE}(d_k)\) & \(8.878\times10^{-3}\) & \(6.608\times10^{-3}\) \\
Recursive IMF \(\operatorname{RMSE}(a^{-k/2}e_k)\) & \(-8.52\times10^{-4}\) & \(1.228\times10^{-2}\) \\
Single-pass \(\operatorname{RMSE}(a^{-k/2}d_k)\) & \(3.0\times10^{-5}\) & \(1.918\times10^{-2}\) \\
\hline
\end{tabular}
\caption{Fitted line coefficients for the no-contamination linear error trends.}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.56\linewidth}rr@{}}
\hline
Metric & Slope \(\beta_1\) & Intercept \(\beta_0\) \\
\hline
Recursive IMF \(\lVert e_k\rVert_\infty\) & \(9.722\times10^{-3}\) & \(5.737\times10^{-3}\) \\
Single-pass \(\lVert d_k\rVert_\infty\) & \(3.104\times10^{-2}\) & \(-2.125\times10^{-2}\) \\
Recursive IMF \(\lVert a^{-k/2}e_k\rVert_\infty\) & \(-3.41\times10^{-4}\) & \(2.244\times10^{-2}\) \\
Single-pass \(\lVert a^{-k/2}d_k\rVert_\infty\) & \(3.211\times10^{-3}\) & \(3.115\times10^{-2}\) \\
\hline
\end{tabular}
\caption{Fitted line coefficients for the no-contamination linear sup-error trends.}
\end{table}

\subsection{Contamination Probability \(p=0.2\)}

For the contaminated case, the real IMF is the robust gradient-descent IMF of the
clean signal. The calculated IMF is the robust gradient-descent IMF of the
contaminated observation. This keeps the comparison same-contrast while testing
robustness under the observation contamination.

\begin{figure}[H]
\centering
\makebox[\linewidth][c]{%
\includegraphics[width=1.12\linewidth,height=0.80\textheight,keepaspectratio]{figures/gd_imf_real_vs_calculated/robust_imf_grid_top.png}%
}
\caption{Contaminated robust comparison for IMF stages 1--5. The labels use
\texttt{window} for the discrete window size; the radius is half-width
\((\texttt{window}-1)/2\).}
\end{figure}

\begin{figure}[H]
\centering
\makebox[\linewidth][c]{%
\includegraphics[width=1.12\linewidth,height=0.72\textheight,keepaspectratio]{figures/gd_imf_real_vs_calculated/robust_imf_grid_bottom.png}%
}
\caption{Contaminated robust comparison for IMF stages 6--9, using the same
column-wise \(y\)-axis ranges as stages 1--5.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated/robust_error_trends.png}
\caption{Contaminated robust recursive and single-pass error RMSE trends over IMF
count, including all IMF stages.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated/robust_sup_error_trends.png}
\caption{Contaminated robust recursive and single-pass sup-error trends over IMF
count. The scaled panels use \(a^{-k/2}\).}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated/robust_error_boxplots.png}
\caption{Contaminated robust signed-error distributions over the time grid. The
scaled panels divide the signed errors by \(a^{k/2}\).}
\end{figure}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.56\linewidth}rr@{}}
\hline
Metric & Slope \(\beta_1\) & Intercept \(\beta_0\) \\
\hline
Recursive IMF \(\operatorname{RMSE}(e_k)\) & \(1.771\times10^{-3}\) & \(1.353\times10^{-2}\) \\
Single-pass \(\operatorname{RMSE}(d_k)\) & \(9.346\times10^{-3}\) & \(1.615\times10^{-2}\) \\
Recursive IMF \(\operatorname{RMSE}(a^{-k/2}e_k)\) & \(-1.401\times10^{-3}\) & \(1.677\times10^{-2}\) \\
Single-pass \(\operatorname{RMSE}(a^{-k/2}d_k)\) & \(-6.58\times10^{-4}\) & \(2.789\times10^{-2}\) \\
\hline
\end{tabular}
\caption{Fitted line coefficients for the contaminated robust error trends.}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.56\linewidth}rr@{}}
\hline
Metric & Slope \(\beta_1\) & Intercept \(\beta_0\) \\
\hline
Recursive IMF \(\lVert e_k\rVert_\infty\) & \(1.598\times10^{-2}\) & \(8.72\times10^{-4}\) \\
Single-pass \(\lVert d_k\rVert_\infty\) & \(3.861\times10^{-2}\) & \(-2.418\times10^{-2}\) \\
Recursive IMF \(\lVert a^{-k/2}e_k\rVert_\infty\) & \(7.6\times10^{-5}\) & \(2.976\times10^{-2}\) \\
Single-pass \(\lVert a^{-k/2}d_k\rVert_\infty\) & \(3.755\times10^{-3}\) & \(4.101\times10^{-2}\) \\
\hline
\end{tabular}
\caption{Fitted line coefficients for the contaminated robust sup-error trends.}
\end{table}

\section{Contamination and Noise Limit Sweep}

The companion notebook
\texttt{gd\_imf\_real\_vs\_calculated\_limits.ipynb} repeats the same
clean-reference comparison over a coarse grid of observation settings. The sweep
uses
\[
\sigma\in\{0.1,0.2,0.4,0.6,0.8\},\qquad
p\in\{0,0.05,0.1,0.2,0.35,0.5,0.65,0.8\},
\]
and contamination scale
\[
\{0.2,0.4,0.6,1,1.5,2,3,4\}.
\]
Each grid point is evaluated with three deterministic coupled repeats, so the
same base Gaussian and contamination draws are reused as the probability and
scale parameters vary. The robust decomposition uses \(H=2\sigma\). This sweep is
descriptive: it shows how the diagnostics degrade across the grid rather than
declaring a pass/fail boundary.

The two aggregate metrics shown below are computed from the recursive IMF errors
only. The mean scaled RMSE is
\[
\frac{1}{K}\sum_{k=1}^K \operatorname{RMSE}(a^{-k/2}e_k),
\]
and the max scaled sup error is
\[
\max_{1\leq k\leq K}\lVert a^{-k/2}e_k\rVert_\infty.
\]
The baseline-relative degradation maps divide each metric by the same method's
\(p=0\) baseline at the same \(\sigma\) and contamination scale.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated_limits/error_surfaces_mean_scaled_rmse.png}
\caption{Limit-sweep error surfaces for the median mean scaled RMSE. The color
scale is shared across both methods and all \(\sigma\) panels, so colors can be
compared directly between linear and robust rows.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated_limits/error_surfaces_max_scaled_sup_error.png}
\caption{Limit-sweep error surfaces for the median max scaled sup error. The
single color scale is shared across both methods and all \(\sigma\) panels for
this metric.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated_limits/baseline_degradation_mean_scaled_rmse.png}
\caption{Baseline-relative degradation for mean scaled RMSE. Each value is the
median metric divided by the corresponding \(p=0\) baseline for the same method,
\(\sigma\), and contamination scale. The centered color scale is shared across
the figure; contour labels mark \(2\times\), \(4\times\), and \(8\times\)
degradation where present.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated_limits/baseline_degradation_max_scaled_sup_error.png}
\caption{Baseline-relative degradation for max scaled sup error. The centered
color scale is shared across both methods and all \(\sigma\) panels, with
\(2\times\), \(4\times\), and \(8\times\) contours shown where the surface
crosses those levels.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_real_vs_calculated_limits/current_setting_slices.png}
\caption{Slices through the current review setting. The left column varies
contamination probability at \(\sigma=0.4\) and contamination scale \(0.6\); the
right column varies contamination scale at \(\sigma=0.4\) and \(p=0.2\). Within
each metric row, the two panels use the same \(y\)-axis scale and show median
with interquartile bands across the three repeats.}
\end{figure}

Across the sweep, the surface plots provide the broad phase diagram, while the
baseline-relative maps show how quickly each method departs from its own
noise-only behavior. The current-setting slices locate the review example
\((\sigma,p,\text{scale})=(0.4,0.2,0.6)\) inside that grid and make the local
dependence on \(p\) and contamination scale easier to read.

\section{Observation Model Comparison}

The preceding sections use the additive contamination model
\[
Y_i = X_i + G_i + M_i C_i,
\]
where \(G_i\) is Gaussian noise, \(M_i\) is the contamination mask, and \(C_i\)
is the signed exponential contamination value. The comparison notebook
\texttt{gd\_imf\_observation\_model\_comparison.ipynb} contrasts this model with
a masked pure-noise contamination model,
\[
Y_i =
\begin{cases}
X_i + G_i, & M_i = 0,\\
G_i + C_i, & M_i = 1.
\end{cases}
\]
Thus contaminated points lose the clean-signal contribution, while ordinary
points keep the same clean signal plus Gaussian noise model. The no-contamination
linear plots are not repeated here because \(p=0\) makes the contamination mask
empty, so both observation models are identical. All figures in this section use
axes or color normalizations computed jointly across the two observation models.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_observation_model_comparison/observations_shared_scale.png}
\caption{Current-setting observations under the additive and masked pure-noise
models. The four panels share one \(y\)-axis scale, so the effect of replacing
mask-selected observations by pure noise is directly comparable.}
\end{figure}

At the current review setting \((\sigma,p,\text{scale})=(0.4,0.2,0.6)\), the
robust contaminated comparison changes because the masked pure-noise model
removes \(X_i\) at contaminated points. The trend and boxplot panels below use
shared \(y\)-axis limits across the two models, so differences in height reflect
metric differences rather than independent rescaling.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_observation_model_comparison/robust_rmse_trends_shared_scale.png}
\caption{Robust contaminated RMSE trends for the additive and masked pure-noise
observation models. Each metric panel uses a shared \(y\)-axis scale across both
models.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_observation_model_comparison/robust_sup_error_trends_shared_scale.png}
\caption{Robust contaminated sup-error trends for the additive and masked
pure-noise observation models. Each metric panel uses a shared \(y\)-axis scale
across both models.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_observation_model_comparison/robust_error_boxplots_shared_scale.png}
\caption{Robust contaminated signed-error distributions under the two
observation models. Raw and scaled panels use symmetric limits around zero,
computed jointly across both models.}
\end{figure}

The limit-sweep comparison repeats the same coarse grid for both observation
models. Error-surface maps use shared logarithmic color scales, baseline-relative
maps use shared centered color scales, and current-setting slices use shared
\(y\)-axis limits across both observation models.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth,height=0.82\textheight,keepaspectratio]{figures/gd_imf_observation_model_comparison/error_surfaces_mean_scaled_rmse_shared_scale.png}
\caption{Mean scaled RMSE surfaces for the additive and masked pure-noise
observation models. The color scale is shared across both models, both methods,
and all \(\sigma\) panels.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth,height=0.82\textheight,keepaspectratio]{figures/gd_imf_observation_model_comparison/error_surfaces_max_scaled_sup_error_shared_scale.png}
\caption{Max scaled sup-error surfaces for the additive and masked pure-noise
observation models. The color scale is shared across both models, both methods,
and all \(\sigma\) panels.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth,height=0.82\textheight,keepaspectratio]{figures/gd_imf_observation_model_comparison/baseline_degradation_mean_scaled_rmse_shared_scale.png}
\caption{Baseline-relative degradation for mean scaled RMSE under both
observation models. The centered color scale is shared across all panels.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth,height=0.82\textheight,keepaspectratio]{figures/gd_imf_observation_model_comparison/baseline_degradation_max_scaled_sup_error_shared_scale.png}
\caption{Baseline-relative degradation for max scaled sup error under both
observation models. The centered color scale is shared across all panels.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/gd_imf_observation_model_comparison/current_setting_slices_shared_scale.png}
\caption{Current-setting sweep slices for both observation models. Within each
metric row, the \(y\)-axis scale is shared across the probability and scale
slices and across both observation models.}
\end{figure}

\end{document}
